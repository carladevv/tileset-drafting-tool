import './App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="app-card">
        <p className="app-eyebrow">tileset-drafting-tool</p>
        <h1>Ready for specification</h1>
        <p className="app-copy">
          This workspace has been reset to a clean application shell. Add the next product spec and we can build
          forward from here.
        </p>
      </section>
    </main>
  )
}

export default App
